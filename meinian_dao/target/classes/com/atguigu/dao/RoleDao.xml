<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.dao.RoleDao">

    <select id="findRolesByUserId" resultMap="roleMap" parameterType="int">
        select r.*
        from t_role r
                 join t_user_role ur on r.id = ur.role_id
        where user_id = #{id}
    </select>

    <resultMap id="roleMap" type="role">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="keyword" property="keyword"/>
        <result column="description" property="description"/>
        <collection property="permissions" column="id" select="com.atguigu.dao.PermissionDao.findPermissionsByRoleId"/>
    </resultMap>

</mapper>